%p#notice= notice
%p
  %strong Name:
  = @product.name

%h3
  = @product.category.name

= image_tag(@product.image, class: 'img-fluid') if @product.image.present?
.w-100
= link_to 'Edit', edit_product_path(@product)
|
= link_to 'Back',  category_products_path(@product)
